# problems.json · 底稿说明

> 本文件是**种子数据**。题号清单来自 LeetCode CN「面试经典 150 题」官方学习计划的网络检索，标题/slug/难度由检索结果 + 人工补全，未经逐题官方核对。
>
> ⚠️ **题单范围不是这个产品的核心——方法论才是**（刷题循环 / 套路分组 / 找入口三步法 / 边界三问 / 默写+SRS，见 `docs/specs/S1-F1/套路映射.md`）。题数 150 还是 151、某道在不在册，都不影响方法论落地；这份种子随用随改即可。

## 说明项

1. **题数 151**：官方「面试经典 150 题」是 150，本底稿 151（多出 `242 有效的字母异位词`）。242 虽不在官方 150，但它是《刷题技巧.md》阶段一的套路代表题，**按「方法论从属于题单」的原则保留**。无需纠结精确 150。
2. **难度/标题/slug**：检索来源可能有出入，`5`/`300` 的 slug（含 "longest" 拼写）、`427`/`909`/`502` 等较新题可抽查。
3. **stage（阶段）映射**：按 PRD 4 阶段 + 我的判断分配，非官方分类。`数学`/`位运算`/`Trie`/`分治`/`设计` 这些 PRD 四阶段没明写的类别，我按就近归入（数学/位运算→阶段1，Trie/设计→阶段2，分治→阶段3）。可按你的方法论调整。
4. **pattern（套路）**：原型里已在 150 范围内的 33 题沿用你标过的 pattern+signal；其余 118 题的 pattern 用 LeetCode 分类名占位，**未细到「套路」粒度**（如「双指针（相向从后）」这种），需你按《刷题技巧.md》细化。
5. **signal（判断信号）**：仅那 33 题有，其余 118 题留空，刷到时再补。
6. **`entry` 字段不在本文件**：按 v1.2 设计，`entry` 由 F2 贴题时从 skeleton 解析存入 problem 记录，不在元数据预填（见 S0-judge / S2-F2）。

## 被剔除的原型题（不在官方 150 内）

原型 `mock.ts` 有 41 题，其中 8 题不在官方 150，本底稿未收录：
`206 反转链表`、`232 用栈实现队列`、`704 二分查找`、`347 前 K 个高频元素`、`643 子数组最大平均数 I`、`424 替换后的最长重复字符`、`78 子集`、`1114 按序打印`（多线程）。

> 其中 `1114` 属多线程题，官方 150 不含多线程。若你想保留这些题自刷，可在 problems.json 末尾追加并标 `optional: true`（类型已支持）。

## 字段

| 字段 | 说明 |
|---|---|
| id | 题号 |
| title | 中文标题 |
| slug | LeetCode CN URL slug |
| difficulty | Easy / Medium / Hard |
| stage | 1-4（用户方法论阶段） |
| pattern | 套路（占位，待细化） |
| signal? | 判断信号（仅 33 题有，可选） |

## 下一步

数据准备与代码实现可并行：S0-scaffold / S0-data-layer 不依赖具体题数，可先开工；本文件在开发前/中由你核对修正即可，不阻塞。