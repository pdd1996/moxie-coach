# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 运行期数据（S0-data-layer）：db.json + 每日备份，均为本机生成
# 前导斜杠锚定到仓库根，避免误忽略 src/data/（seed.ts、problems.json 等源码）
/data/

# Pyodide 本地打包（S0-judge D2）：约 10MB，由 setup 脚本下载到 public/pyodide/，不入库
public/pyodide/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 冒烟运行产物（截图/日志，由 scripts/smoke-s3.mjs 生成，不入库）
scripts/smoke-A.png
scripts/*.out

.zcode
gui-test-screenshots